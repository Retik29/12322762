# Campus Notify Frontend

React frontend for the MERN notification assignment.

## Run

```bash
npm install
npm run dev
```

The app runs on:

```text
http://localhost:3000
```

Make sure the backend is running on `http://localhost:4000`.
